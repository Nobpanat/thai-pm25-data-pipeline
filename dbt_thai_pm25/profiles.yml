dbt_thai_pm25:
  outputs:
    dev:
      dataset: "{{ env_var('DBT_DATASET_DEV', 'your-dataset-name') }}"
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: ../terraform/keys/credentials.json
      location: asia-southeast1
      method: service-account
      priority: interactive
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      threads: 4
      type: bigquery
  target: dev
